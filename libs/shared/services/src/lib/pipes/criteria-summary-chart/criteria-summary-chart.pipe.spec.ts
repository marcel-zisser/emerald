import { CriteriaSummaryChartPipe } from './criteria-summary-chart.pipe';

describe('CriteriaSummaryChartPipe', () => {
  it('create an instance', () => {
    const pipe = new CriteriaSummaryChartPipe();
    expect(pipe).toBeTruthy();
  });
});
